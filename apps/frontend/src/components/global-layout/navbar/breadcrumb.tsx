import { MaterialIcon } from "@/components/material-icon";
export default function Breadcrumb({ items = ["Creative Universe", "Global Layout"] }: { items?: string[] }) { return <div className="relative flex items-center gap-1">{items.map((item, index) => <span key={`${item}-${index}`} className="flex items-center gap-1"><span className={`whitespace-nowrap font-sans text-sm leading-none ${index === items.length - 1 ? "font-medium text-[#3b4446]" : "font-normal text-[#aeb6b8]"}`}>{item}</span>{index < items.length - 1 && <span className="flex size-6 items-center justify-center"><MaterialIcon name="chevron_right" size="sm" className="text-[#aeb6b8]" /></span>}</span>)}</div>; }
