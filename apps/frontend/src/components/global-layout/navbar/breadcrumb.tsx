import { MaterialIcon } from "@/components/material-icon";
export default function Breadcrumb({ items = ["Creative Universe", "Global Layout"], dark = false }: { items?: string[]; dark?: boolean }) { return <div className="relative flex items-center gap-1">{items.map((item, index) => <span key={`${item}-${index}`} className="flex items-center gap-1"><span className={`whitespace-nowrap font-sans text-sm leading-none ${index === items.length - 1 ? `font-medium ${dark ? "text-white" : "text-[#3b4446]"}` : dark ? "font-normal text-[#7b7b7b]" : "font-normal text-[#aeb6b8]"}`}>{item}</span>{index < items.length - 1 && <span className="flex size-6 items-center justify-center"><MaterialIcon name="chevron_right" size="sm" className={dark ? "text-[#7b7b7b]" : "text-[#aeb6b8]"} /></span>}</span>)}</div>; }
