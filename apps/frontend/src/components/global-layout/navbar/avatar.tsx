export default function Avatar({ name = "Creative Universe", src, state = "Default" }: { name?: string; src?: string | null; state?: "Default" | "Focus" }) { const initials = name.split(/\s+/).slice(0, 2).map((part) => part[0]).join("").toUpperCase(); const border = state === "Focus" ? "border border-[rgba(0,0,0,0.3)]" : ""; return src ? <div className={`relative size-8 shrink-0 overflow-hidden rounded-lg ${border}`}><img src={src} alt={name} className="absolute inset-0 size-full rounded-lg object-cover" /></div> : <div className={`relative flex size-8 shrink-0 items-center justify-center overflow-hidden rounded-lg bg-[#d9d9d9] ${border}`}><span className="font-sans text-[11px] font-semibold leading-none text-[#222]">{initials}</span></div>; }
