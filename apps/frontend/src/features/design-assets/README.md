# Design Assets Feature

- Page route: `/design-assets`.
- API prefix yang dicadangkan: `/api/v1/design-assets`.
- Status: experimental.
- Ownership: eksperimen katalog atau pengelolaan asset desain.

Belum ada endpoint aktif; UI tidak boleh mensimulasikan fungsi production.

Public API module: `api/index.ts`. Module hanya mengekspos status `experimental-no-backend` dan tidak melakukan request.
