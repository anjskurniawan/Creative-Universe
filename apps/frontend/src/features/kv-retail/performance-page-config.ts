/**
 * Single source of truth for the dedicated KV Retail performance screen.
 * Heading and navigation label are intentionally separate: the short label is
 * used in menus where space is limited.
 */
export const KV_RETAIL_PERFORMANCE_PAGE = {
  title: "Performa",
  navLabel: "Performa",
  parentTitle: "KV Retail Task",
} as const;
