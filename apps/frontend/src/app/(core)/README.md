# Core Route Group

Route group aktif untuk dashboard, profile, settings, users, roles, messages, dan maintenance. Route group `(core)` tidak muncul pada URL.

Core menggunakan `core/layouts/core-shell.tsx` dan tidak lagi memiliki percabangan layout untuk Sub-App.
