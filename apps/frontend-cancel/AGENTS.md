# Cancelled frontend snapshot

This directory contains the permanently cancelled clean-rebuild experiment.

- Treat every file in this directory as read-only.
- Do not implement fixes, features, refactors, dependency updates, generated-file updates, builds, formatting, or documentation changes here.
- Do not import, symlink, alias, copy runtime source from, or include this directory in the active frontend build graph.
- The only active frontend is `apps/frontend/`.
- This directory may be deleted only after a separate explicit instruction from the user.
