# Legacy Application - Laravel Livewire (Read-Only)

Folder ini berisi snapshot read-only dari aplikasi monolit Laravel Livewire sebelum dilakukan refaktor arsitektur headless.

> [!IMPORTANT]
> - **DILARANG MENGUBAH KODE** di dalam folder ini. Seluruh modifikasi/pengembangan fitur harus dilakukan di dalam folder `apps/backend` (Laravel REST API) atau `apps/frontend` (Next.js).
> - Folder ini tidak masuk ke dalam pipeline build, testing, maupun deployment aktif.
> - Snapshot ini tidak menyertakan file konfigurasi `.env`, folder dependensi `vendor/` atau `node_modules/`, file cache/logs/session, maupun data upload milik pengguna untuk alasan keamanan dan kebersihan repository.

Commit terakhir sebelum pembekuan: `750d491f0a4f5240ae27fe3f47e1839f1fbd5972` (Tag: `legacy-livewire-final`).
